.DS_Store
node_modules/
.claude/settings.local.json

# IDE
.idea/

# Android / Gradle
.gradle/
build/
local.properties
# Config Firebase : contient l'API key du projet ; injectée localement / via secret CI (dépôt public).
google-services.json
*.apk
*.aab
*.keystore
.cxx/
captures/
.kotlin/
# Lib native H3 x86_64 cross-compilée localement (NDK) pour les runs sur émulateur x86.
# Dev-only, non publiée upstream par com.uber:h3 — chaque poste la régénère au besoin.
app/src/debug/jniLibs/
